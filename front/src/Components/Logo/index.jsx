import React from 'react';
import styled from '@emotion/styled';

const LogoText = styled.p`
  font-size: 25px;
  font-weight: 700;
`;

const Logo = () => {
  return <LogoText>Rebrand</LogoText>;
};

export default Logo;
